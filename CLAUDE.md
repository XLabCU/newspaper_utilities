#to do

Make sure the run_pipeline script passes the correct OCR json file from the process stage to the segment_articles stage.

Take the first three words as the headline in any snippet

Make sure the jpgs can be viewed in the dashboard

Make sure the network visualization d3 container is working properly

Make sure the researcher's notepad feature (floating text notepad to write down observations) is working.

Make sure all javascript external scripts are called in the dashboard generator.